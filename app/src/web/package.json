{
  "scripts": {
    "compile": "tsc -p tsconfig.json && sass --no-source-map --style compressed --update assets/css:assets/css"
  },
  "dependencies": {
    "typescript": "^3.3.1",
    "sass": "^1.19.0"
  }
}
